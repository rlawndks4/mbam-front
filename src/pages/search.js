import UserLayout from "src/layouts/UserLayout";

const Search = () => {

    return (
        <>

        </>
    )
}
Search.getLayout = (page) => <UserLayout>{page}</UserLayout>;

export default Search;